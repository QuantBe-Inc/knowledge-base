---
title: 'Math'
description: 'From Basic Arithmetic to Advanced Stats: Your All-in-One Math Tool.'
icon: 'calculator-simple'
---

## Mathematics and Statistics

The Mathematics and Statistics function encompasses a diverse range of capabilities, including fundamental operations like addition, as well as advanced statistical analyses such as standard deviation and linear regression. Whether users are calculating basic arithmetic sums or delving into sophisticated statistical modeling, this versatile function provides users with the tools for comprehensive mathematical and statistical analyses.

<Frame className="mx-auto">
    <img style={{height: "300px"}} src="/images/Strategy_Function/Math.png" /> 
</Frame>

<ResponseField name="Function" type="" required>
  Choose from a mathematical function (Add, Standard Deviation, Beta, Linear Regression & Exp)
</ResponseField>




  <Tabs>
  <Tab title="STD">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

  </Tab>
  <Tab title="VAR">
    <ResponseField name="Input" type="" required>
    Select from a dataset that is a range saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

  </Tab>
  <Tab title="SUM">
    <ResponseField name="Input" type="" required>
    Select from a dataset that is a range saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

  </Tab>
  <Tab title="EXP">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="CEIL">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="FLOOR">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="LN">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

  </Tab>
  <Tab title="LOG10">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

  </Tab>
  <Tab title="SQRT">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField> 

  </Tab>
  <Tab title="MAX">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField> 

   <ResponseField name="Time Ref" type="" required>
    Users are required to provide a name for the time reference of this data.
    </ResponseField> 

 </Tab>
  <Tab title="MIN">
    <ResponseField name="Input" type="" required>
    Select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField> 

   <ResponseField name="Time Ref" type="" required>
    Users are required to provide a name for the time reference of this data.
    </ResponseField> 

  </Tab>
  <Tab title="ADD">
    <ResponseField name="Input 1&2" type="" required>
    Users are required to input values, and they have the option to either manually enter the values or select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="SUB">
    <ResponseField name="Input 1&2" type="" required>
    Users are required to input values, and they have the option to either manually enter the values or select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="DIV">
    <ResponseField name="Input 1&2" type="" required>
    Users are required to input values, and they have the option to either manually enter the values or select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>

</Tab>
  <Tab title="MULT">
    <ResponseField name="Input 1&2" type="" required>
    Users are required to input values, and they have the option to either manually enter the values or select from a dataset saved in the database.
    </ResponseField>

    <ResponseField name="Define Results" type="" required>
    Users are required to provide a name for their data.
    </ResponseField>


  </Tab>
</Tabs>